// ! Template String: it is a ES6 Features 
// It allows us embedded expressions. We can use multiline string. It works same as pre tag in html.
// It use backtick (next button to 1 key in keyboard).​​
    // Syntax: ​
        // ` ${}
        //     ...
        //     ...
        // `
    // Example:
    var a = 'Welcome';
    console.log(` Hello ${a}, User
    How are you Doing?
    Thanks & ${a}`);
