// ! Type Conversion​: Means we can explicitly convert the one variable #dt into another variable #dt.​

// 2 Types of Type Coercion​
    // 1. Implicit Coercion​
    // 2. Explicit Coercion​

// There are following Type Conversion​
// 1. String Conversion​
// 2. Number Conversion​
// 3. Boolean Conversion​

// 2. Explicit Conversion & Coercion 
// 2.1 String Conversion: It converts into string #dt​
    // There are following way of string conversion​
    // 2.1.1 String();​
        // Example​
        let k =23;
        k = String(k);

    // 2.1.2 toString();​
        // Example​
        let l =23;
        l = l.toString();

    // 2.1.3 varName + "";​
        // Example​
        let m = 23;
        m = m + "";

    // 2.2 Number Conversion​:   It converts into number #dt​
    // There are following way of Number conversion​    
    // 2.2.1 Number();​    
        // Example​
        let n ="23";
        n = Number(n);

    // 2.2.2 + "varValue";​    
        // Example​
        let o = "23";
        o = +"23";

    // 2.3 Boolean Conversion: 
    // 2.3.1 Boolean();
        let p = 575755;
        p=Boolean(p);

    // 2.3.2 n 
        let q = 866678;
        q = 866678n;